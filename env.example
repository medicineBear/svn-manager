# 数据库配置
# Docker Compose 环境变量（开发环境默认值）
POSTGRES_DB=svnmanager
POSTGRES_USER=svnmanager
POSTGRES_PASSWORD=svnmanager

# Spring Boot 应用环境变量（开发环境默认值）
DB_USERNAME=svnmanager
DB_PASSWORD=svnmanager

# 生产环境配置示例（取消注释并修改为实际值）
# DB_HOST=your-db-host
# DB_PORT=5432
# DB_NAME=svnmanager
# DB_USERNAME=your-username
# DB_PASSWORD=your-strong-password

# JWT 配置（生产环境必须设置）
# 开发环境可以使用默认值，但生产环境必须设置强随机密钥（至少32字符）
# 生成密钥示例（Linux/Mac）: openssl rand -base64 32
# 生成密钥示例（PowerShell）: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# JWT_SECRET=your-strong-random-secret-key-at-least-32-characters

# CORS 配置（生产环境必须设置）
# 多个域名用逗号分隔，例如: https://example.com,https://www.example.com
# CORS_ALLOWED_ORIGINS=https://yourdomain.com

# 日志配置（可选）
# LOG_FILE_PATH=./logs/svn-manager.log

